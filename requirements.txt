streamlit>=1.39
anthropic>=0.40
pandas>=2.2
numpy>=1.26
plotly>=5.20
statsmodels>=0.14
python-dotenv>=1.0

# Google Sheets live sync (optional but recommended — wire up via Settings)
google-auth>=2.30
google-auth-oauthlib>=1.2
google-api-python-client>=2.140

# Stripe live sync (optional — wire up via Settings)
stripe>=10.0

# Markdown → HTML conversion for the Save-as-report feature
markdown>=3.5

# Document import: read Excel workbooks, extract text from PDFs and Word docs
openpyxl>=3.1
pypdf>=4.0
python-docx>=1.1

# Document export: generate PDF reports
fpdf2>=2.7.9

# Slack bot integration (optional — only needed when running slack_bot.py)
slack_bolt>=1.18

# Scheduled tasks daemon (optional — only needed when running scheduler.py)
APScheduler>=3.10
